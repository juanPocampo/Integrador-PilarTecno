import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    primary: {
      // Deep orange.
      main: "#E2725B",
      contrastText: "#fffffc",
    },
    secondary: {
      // Light Green.
      main: "#F57E34",
      contrastText: "#1b5e20",
    },
  },
});
